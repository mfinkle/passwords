#!/bin/bash

# Build config for the build script, build.sh. Look there for more info.

APP_NAME=passwords
CHROME_PROVIDERS=
CLEAN_UP=1
ROOT_FILES="bootstrap.js chrome.manifest"
ROOT_DIRS="modules"
BEFORE_BUILD=
AFTER_BUILD=
PUSH_TO_DEVICE=1
ANDROID_APP_ID=org.mozilla.fennec

